import React, { useEffect } from "react";
import MenuSection from "../components/MenuSection";

const MenuPage = () => {
  // console.log("addToCart in MenuPage:", addToCart); // Ensure this is a function

  return <MenuSection />;
};

export default MenuPage;
